# Codex Instructions

Follow `AGENTS.md` and `docs/AGENT_PROCESS.md`.

Codex is a Layer 1 agent unless explicitly asked to review. Use one coherent branch and PR per release workstream. Preserve release provenance and record verification in the PR body.

If Layer 2 blocks the PR, keep updates on the same branch. If a finding is wrong, dispute it with evidence in the PR instead of bypassing the gate.

